{
  "name": "liwi-monorepo",
  "private": true,
  "description": "db abstraction",
  "author": "Christophe Hurpeau <christophe@hurpeau.com> (http://christophe.hurpeau.com/)",
  "license": "ISC",
  "repository": "https://github.com/liwijs/liwi.git",
  "homepage": "https://github.com/liwijs/liwi",
  "type": "module",
  "engines": {
    "node": ">=14.13.1"
  },
  "packageManager": "yarn@3.3.0",
  "workspaces": [
    "packages/*",
    "@todo-example/*"
  ],
  "scripts": {
    "build": "yarn clean:build && rollup --config rollup.config.mjs && yarn run build:definitions",
    "build:definitions": "tsc -b",
    "checks": "node scripts/check-packages.js",
    "clean": "rm -Rf docs dist test/node6 coverage",
    "clean:build": "yarn workspaces foreach --parallel -A run clean:build",
    "lint": "yarn run lint:prettier && yarn run lint:eslint",
    "lint:eslint": "eslint --report-unused-disable-directives --resolve-plugins-relative-to . --quiet .",
    "lint:prettier": "pob-root-prettier --check .",
    "lint:prettier:fix": "pob-root-prettier --write .",
    "postinstall": "pob-root-postinstall",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest",
    "tsc": "tsc -b",
    "version": "YARN_ENABLE_IMMUTABLE_INSTALLS=false yarn && git add yarn.lock",
    "watch": "yarn clean:build && rollup --config rollup.config.mjs --watch"
  },
  "prettier": "@pob/root/prettier-config",
  "commitlint": {
    "extends": [
      "@pob/commitlint-config"
    ]
  },
  "jest": {
    "cacheDirectory": "./node_modules/.cache/jest",
    "testEnvironment": "node",
    "testMatch": [
      "<rootDir>/(packages|@todo-example)/*/(src|lib)/**/__tests__/**/*.(ts|js|cjs|mjs)?(x)",
      "<rootDir>/(packages|@todo-example)/*/(src|lib)/**/*.test.(ts|js|cjs|mjs)?(x)"
    ],
    "extensionsToTreatAsEsm": [
      ".ts",
      ".tsx"
    ]
  },
  "resolutions": {
    "next@13.0.4": "patch:next@npm%3A13.0.4#./.yarn/patches/next-npm-13.0.4-fb87a1424a.patch"
  },
  "resolutionsExplained": {
    "next@13.0.4": "Patch next to prevent it to change tsconfig.json"
  },
  "devDependencies": {
    "@babel/core": "7.20.5",
    "@pob/commitlint-config": "5.1.0",
    "@pob/eslint-config": "50.0.0",
    "@pob/eslint-config-typescript": "50.0.0",
    "@pob/eslint-config-typescript-react": "50.0.0",
    "@pob/lerna-light": "6.1.0",
    "@pob/root": "7.2.0",
    "@types/jest": "29.2.3",
    "check-package-dependencies": "3.11.0",
    "eslint": "8.28.0",
    "jest": "29.3.1",
    "pob-babel": "35.3.0",
    "typescript": "4.9.3"
  }
}
